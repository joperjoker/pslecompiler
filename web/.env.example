# Optional: when set, the app serves the bank from Supabase instead of the
# bundled seed. Leave blank to run fully offline with the seed + localStorage.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
